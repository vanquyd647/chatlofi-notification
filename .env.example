# Port (Render sẽ tự động set)
PORT=3000

# Firebase Service Account JSON (paste toàn bộ nội dung file service-account-key.json)
# Trên Render: Set as Environment Variable
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"chatlofi-9c2c8",...}

# Hoặc dùng file path cho local development
# GOOGLE_APPLICATION_CREDENTIALS=../config/service-account/service-account-key.json
